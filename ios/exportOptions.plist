<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>method</key>
    <string>app-store</string>
    <!-- Xcode's default for method=app-store is to rewrite CFBundleVersion at
         export time, asking App Store Connect for a number instead of using the
         one that was archived. That silently divorces what ships from what
         pubspec.yaml declares: an archive built at 32 exported as 33. The build
         number is the repository's decision, so this turns that off. -->
    <key>manageAppVersionAndBuildNumber</key>
    <false/>
    <key>uploadBitcode</key>
    <false/>
    <key>uploadSymbols</key>
    <true/>
</dict>
</plist>